{{!-- Page to create a new editor-document-status --}}
<div class="new">
  <h2 class="title">{{t (humanize 'New editor-document-status')}}</h2>

  {{!--
    Actions to save new editor-document-status
    or delete new editor-document-status.
  --}}
  <div class="actions">
    <button type="button" class="btn save" title="Save new editor-document-status" {{action 'save' model}}>Save</button>
    <button type="button" class="btn cancel" title="Cancel" {{action 'cancel' model}}>Cancel</button>
  </div>

  {{!-- Table to show details of editor-document-status. --}}
  <table>
    <thead>
      <tr>
        <th>Attribute</th>
        <th>Data</th>
      </tr>
    </thead>
    <tbody>
      {{!-- Creating rows for attributes depending on the type of property --}}
            <tr>
        <td>name</td>
<td align='middle'>{{handle-attribute model=model attribute='name' editable=true}}</td>
      </tr>
      
      
    </tbody>
  </table>
</div>
